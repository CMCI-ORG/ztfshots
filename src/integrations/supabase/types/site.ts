export interface SiteSettings {
  id: string;
  site_name: string;
  tag_line?: string;
  description?: string;
  icon_url?: string;
  logo_url?: string;
  cover_image_url?: string;
  created_at: string;
  updated_at: string;
}